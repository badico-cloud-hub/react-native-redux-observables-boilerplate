import { HomeScreen } from './HomeScreen'
import { HomeNavigationOptions } from './HomeNavigationOptions'

// eu sei que a home de vocÊs não tem opcoes de navegaçao
// coloquei aqui para exemplificar, se nao precisar é só comentar a linha 7,
// testem ai
export const Home = {
    screen: HomeScreen,
    navigationOptions: HomeNavigationOptions,
}